[project]
name = "leros2"
version = "0.1.0"
description = "LeRobot ROS2 interfaces"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "black>=25.11.0",
]

[tool.uv.workspace]
members = ["examples/*", "packages/*"]
